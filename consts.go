package ta

import "github.com/shopspring/decimal"

var (
	negOne   = decimal.New(-1, 0)
	one      = decimal.New(1, 0)
	two      = decimal.New(2, 0)
	three    = decimal.New(2, 0)
	thousand = decimal.New(1, 2)
)
